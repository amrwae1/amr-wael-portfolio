# Normalise line endings so the repo is identical on Windows, macOS, and CI.
* text=auto eol=lf

# Binary assets — never diffed or line-ending converted.
*.webp binary
*.png  binary
*.jpg  binary
*.ico  binary
